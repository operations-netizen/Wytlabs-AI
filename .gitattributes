# Normalise line endings so Windows and macOS/Linux collaborators do not
# produce whole-file diffs from CRLF/LF churn alone.
* text=auto eol=lf

# Binary assets: never touch these.
*.png binary
*.jpg binary
*.woff2 binary
*.mp4 binary
